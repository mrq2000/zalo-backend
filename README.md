## Run môi trường development
* docker-compose up -d
* cp .env.example .env
* npm install
* npm run migrate:run
* npm run seed:run
* Mã nguồn được map vào folder /opt/app

## Run 
* npm run dev (realtime)
